"""Service layer — pure Python modules with no Flask or framework dependencies.

These can be imported from Blueprint modules, the worker daemon, or tested
independently. Each module handles one external integration.
"""
